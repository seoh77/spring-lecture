<!--
스프링부트 3.1부터 기본 로그 레벨을 INFO로 설정하여 로그를 확인할 수 없는데,
아래와 같이 설정하면 기본 로그 레벨을 DEBUG로 설정하여 강의 내용과 같이 로그를 확인할 수 있음

강의 뒤에 나오는 CoreApplication 처럼 스프링부트를 실행할 때는 이 파일을 제거하거나,
또는 <root level="DEBUG"> 부분을 <root level="INFO">로 변경하면 강의 내용처럼 로그 확인 가능
-->

<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -%kvp- %msg%n</pattern>
        </encoder>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>